<h2>Detail User</h2>
<div class="container">
  <table class="table">
    <thead>
      <tr>
        <th>Username</th>
        <th>Fullname</th>
        <th>Address</th>
        <th>Sex</th>
        <th>Email</th>
        <th>Group</th>
      </tr>
    </thead>
    <tbody>
      <% if(data.user) { %>
      <tr>
        <td><%= data.user.username %></td>
        <td><%= data.user.fullname %></td>
        <td><%= data.user.address %></td>
        <td><%= data.user.sex == '1' ? 'nam' : 'nu' %></td>
        <td><%= data.user.email %></td>
        <td><%= data.user.groupid == 1 ? 'admin' : 'user' %></td>
      </tr>
      <% } else { %>

      <td>ko co user</td>
      <% } %>
    </tbody>
  </table>
</div>
